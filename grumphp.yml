grumphp:
  stop_on_failure: true
  process_timeout: 160
  tasks:
    phpcs:
      standard: 'phpcs.ruleset.xml'
      encoding: utf-8
      triggered_by: [ 'php' ]
    phpunit: ~
    phpstan:
      configuration: phpstan.neon
      triggered_by: [ 'php' ]
      memory_limit: '-1'
